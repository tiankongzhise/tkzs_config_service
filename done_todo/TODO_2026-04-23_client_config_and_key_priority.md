# TODO (2026-04-23): 客户端配置友好性与RSA私钥优先级

## 背景
- 需要澄清 `encrypted_aes_key` 的语义，避免误解为“传私钥/传公钥”。
- `config.py` 作为 PyPI 包内文件，不适合让用户靠修改源码来改默认配置。
- 登录时需要支持用户显式指定 RSA 私钥路径，并定义明确优先级。

## 需求拆解
- [x] 说明 `upload_config` / `update_config` 的 `encrypted_aes_key` 是什么
- [x] 增加运行时配置函数，支持安装后通过代码修改默认配置
- [x] 增加登录私钥路径优先级：登录参数 > 配置类自定义 > 默认逻辑
- [x] 在读取不到公钥文件时，允许由私钥动态推导公钥用于上传/更新
- [x] 增加对应 tests case
- [x] 更新 README 使用说明 case

## 验收标准
- [x] 用户无需改包内源码，可通过 `configure_client(...)` 覆盖默认配置
- [x] `client.login(..., private_key_path=...)` 可强制指定私钥路径
- [x] 仅配置私钥路径时，上传/更新仍可工作（公钥可从私钥推导）
- [x] 单元测试覆盖优先级行为与运行时配置行为
